# ======================================================================
# GitHub 专用代理配置
#
# 说明：
# - 由主 %USERPROFILE%\.gitconfig 通过 includeIf 条件化加载，仅对 remote.*.url 为 https://github.com/* 的仓库生效。
# - 使用本地 socks5 代理访问 GitHub。
# ======================================================================

[http "https://github.com"]
    proxy = socks5://127.0.0.1:10808  # GitHub 代理（本地 socks5）

